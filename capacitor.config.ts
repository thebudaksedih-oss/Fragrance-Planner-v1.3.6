import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fragranceplanner.app',
  appName: 'Fragrance Planner',
  webDir: 'dist'
};

export default config;
